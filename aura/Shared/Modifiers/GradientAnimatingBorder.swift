import SwiftUI

/// Retired: the launcher's animated gradient sweep is gone, the chrome is flat.
struct GradientAnimatingBorder: ViewModifier {
    let color: Color
    let trigger: Bool

    func body(content: Content) -> some View {
        content
    }
}
