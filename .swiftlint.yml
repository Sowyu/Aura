excluded:
  - Pods
  - Carthage
  - .build
  - DerivedData
  - build
  - Build
  - "*.xcworkspace"
  - "*.xcodeproj"
  - fastlane/report.xml
  - fastlane/Preview.html
  - fastlane/screenshots
  - fastlane/test_output

opt_in_rules:
  - empty_count
  - force_unwrapping
  - implicitly_unwrapped_optional
  - weak_delegate

# Disabled rules to avoid conflicts with SwiftFormat
disabled_rules:
  - trailing_comma # SwiftFormat handles this with --commas inline
  - vertical_whitespace # SwiftFormat handles whitespace
  - opening_brace # SwiftFormat handles brace style with --allman false
  - statement_position # SwiftFormat handles statement positioning
  - redundant_string_enum_value # Let SwiftFormat handle redundancy
  - sorted_imports # SwiftFormat has this disabled

# Rule configurations
#
# Error thresholds sit just above where the codebase actually is today, so the
# gate blocks growth without demanding refactors before CI can reach the build
# and tests at all (this fork's Actions were dormant until 2026-08-26, so the
# original thresholds were never enforced). Tighten as the debt is paid down.
line_length:
  warning: 120
  # error: 140

file_length:
  warning: 500
  error: 1200

function_body_length:
  warning: 50
  error: 100

type_body_length:
  warning: 300
  error: 700

identifier_name:
  min_length:
    warning: 3
    error: 1

cyclomatic_complexity:
  warning: 10
  error: 20

nesting:
  type_level:
    warning: 2
    error: 3
  function_level:
    warning: 2
    error: 3

force_unwrapping:
  severity: warning

implicitly_unwrapped_optional:
  severity: warning

custom_rules:
  no_print_statements:
    name: "No Print Statements"
    regex: "print\\("
    message: "Use logger instead of print statements"
    severity: warning
